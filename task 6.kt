fun main() {
    // Create an array of 5 names
    val names = arrayOf("Zara", "Isa", "Ismail", "Adam", "Faruk")

    // Loop through the array and print each name with a greeting
    for (name in names) {
        println("Hello, $name!")  // Print a greeting with each name
    }
}
